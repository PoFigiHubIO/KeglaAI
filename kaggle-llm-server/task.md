- `[ ]` Stage 1: Запуск и проверка текущей базы (Уже готово на GitHub)
  - `[ ]` Запустить Kaggle Notebook и обновить репозиторий (`!git -C KeglaAI pull`).
  - `[ ]` Вписать секреты `NGROK_AUTHTOKEN` и `NGROK_AUTHTOKEN_2` в менеджер Secrets на Kaggle.
  - `[ ]` Запустить ячейку последовательного старта обеих моделей (GPU 0 на порту 8080 и GPU 1 на порту 8081).
  - `[ ]` Проверить доступность по постоянным ссылкам:
    - `https://28dcb02c7bfa-1965096042333252791.ngrok-free.app`
    - `https://sabbath-lucid-molar.ngrok-free.dev`
- `[x]` Stage 2: Разработка SSE MCP сервера генерации на GPU 1 (FastAPI + Diffusers + SSE)
  - `[x]` Создать FastAPI-обертку, слушающую порт `8081`.
  - `[x]` Интегрировать загрузку FLUX.1 Dev (GGUF/FP8) с LoRA `aidmaNSFWunlock`.
  - `[x]` Интегрировать Wan 2.2 Remix (FP8/GGUF) для Image-to-Video.
  - `[x]` Настроить менеджер VRAM (переменные PyTorch, VAE tiling, принудительный сбор мусора при смене моделей).
  - `[x]` Написать SSE-эндпоинты (`GET /sse` и `POST /messages`) для интеграции протокола MCP.
- `[ ]` Stage 3: Telegram Bot Agent Loop с поддержкой динамических MCP
  - `[ ]` Проектирование базы данных бота (SQLite) для истории диалогов и конфигураций MCP-серверов.
  - `[ ]` Разработка ядра Telegram-бота (Python, `python-telegram-bot`, асинхронный цикл).
  - `[ ]` Реализация клиента MCP и Tool-Calling (подключение к SSE-ссылке GPU 1).
  - `[ ]` Разработка модуля авто-установки (Dynamic Installer: `search_mcp` и `install_mcp`).
  - `[ ]` Реализация интерфейса ТГ-команд (`/mcp_list`, `/mcp_enable`, `/mcp_disable`, `/mcp_search`).
- `[ ]` Stage 4: Разработка бесшовного Failover (24/7 работа серверов)
  - `[ ]` Написать Cloudflare Worker (маршрутизатор трафика) с интеграцией Cloudflare KV.
  - `[ ]` Реализовать скрипты Rclone для синхронизации БД (`agent.db`) с Yandex Disk / Google Drive.
  - `[ ]` Написать скрипт-таймер, который через 8 часов 50 минут вызывает API Kaggle (`kaggle kernels push`) для старта следующей ноды.
  - `[ ]` Реализовать API-метод `/v1/handover/complete` для плавного гашения старого инстанса.
  - `[ ]` Настроить кольцевые секреты на 7 аккаунтах Kaggle.
- `[ ]` Stage 5: Разработка новых фич (LoRA Hot-Swap, FFmpeg Compression, Process Warden)
  - `[ ]` Реализовать динамическое скачивание и горячую загрузку/выгрузку LoRA-стилей (`load_style_lora`) на GPU 1.
  - `[ ]` Настроить автоматическое сжатие видео через `ffmpeg` (H.265/AV1) перед отправкой.
  - `[ ]` Реализовать фоновый модуль проверки здоровья процессов (Process Warden) в коде Telegram-бота для автозапуска упавших MCP.
- `[ ]` Stage 6: Интеграция и тестирование
  - `[ ]` Подключить встроенный Web UI от `llama.cpp` к нашему SSE-туннелю на GPU 1 и проверить генерацию картинок в чате.
  - `[ ]` Проверить генерацию картинок и видео в Telegram-боте.
  - `[ ]` Симулировать ротацию и убедиться, что база переписки перетекает между аккаунтами без остановки API.
